services:
  - type: web
    name: churn-predict-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - fromGroup: retainiq-supabase-env
    plan: free